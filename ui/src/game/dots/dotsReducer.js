const initialState = {};

export default (state = initialState, action) => {
  switch (action.type) {
    case "INIT_DOTS_MATCH":
      break;
    case "SEND_MOVE":
      break;
    case "GAME_MOVE_RESPONSE":
      break;
  }
};
